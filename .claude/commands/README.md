# .claude/commands/

Project-scoped slash commands — committed to the repo, available to all contributors.

Personal commands (/checkpoint, /init-project) live in ~/.claude/commands/ instead.

Add commands here for project-specific workflows: /deploy, /migrate, /seed-db, etc.
